# Niri Keybinds

View and manage your Niri keybinds from a panel.

## Commands

- `>niri keybinds` or `>keybinds` - Open panel
- Click bar widget to open panel

## Requirements

- Niri config at `~/.config/niri/config.kdl` or custom path in settings